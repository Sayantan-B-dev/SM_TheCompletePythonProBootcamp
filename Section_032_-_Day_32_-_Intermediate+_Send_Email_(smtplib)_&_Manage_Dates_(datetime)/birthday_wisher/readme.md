# Make sure to have a `.env` file in the same directory as the script
```
EMAIL=your-email@gmail.com
PASSWORD=your-app-password

```

## You can use `python -B main.py` to run the script without creating .pyc files

## You can use `pip install -r requirements.txt` to install the required packages 